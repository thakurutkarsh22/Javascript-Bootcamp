// Responsive Design

// 1. Media Queries

// 2. Bootstrap...

// I need to make sure the elements on the page is Changing depending on the Screen size,

// @media in css

// GROUND  RULES for Responsive Design

/*
    1. DO NOT USE ABSOLUTE UNITS ()
    2. use Relative Untis (em, rem, % vh, vw)
    3. Containers (div) should be fluid. (eg: Flex box).
    4. MAx no of time do not try to fix Width and Height.
    5. Desgin Moblie FIRST -> 
        Add Breakpoints to have Support for Mobile.

   Breakpoints: is a point where in we want our CSS to shift from one state to antoher.
   
   use @media to add breakpoint to the page.


   320px - 480px -> Mobile phone
   480px - 640px -> Mini tablet (Ipad mini)
   640px - 768px -> Tablet (i pad pro)
   960px - 1024px -> laptop, Desktops
   1024px > -> Hi resolution latops and desktops.


   RULES 
   1. ->  CSS  at rules  (@)

   is a statement that provides the CSS with instructions to perform the behaviour.

   @import 
   @font-face
   @keyframes


   2. -> at Media  @media

   to define the breakpoint.

   @media [media-type]([media-feature]) {
    // Normal CSS
   }


   [media-type] -> Optional

   


*/
